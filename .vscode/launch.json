{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "cppdbg",
      "request": "launch",
      "preLaunchTask": "Build",
      "name": "Debug on macOS",
      "program": "${workspaceFolder}/build/debug", // Adjust the executable name as needed
      "args": [],
      "cwd": "${workspaceFolder}",
      "MIMode": "lldb"
    },
    {
      "type": "cppvsdbg",
      "request": "launch",
      "preLaunchTask": "Build",
      "name": "Debug on Windows",
      "program": "${workspaceFolder}/build/debug.exe",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
